name: shared_prefs_manager
description: A Dart package for easily using SharedPreferences. Provides access to the majority of SharedPreference's API.
version: 0.0.1
homepage: https://github.com/aidanlok/shared_prefs_manager
repository: https://github.com/aidanlok/shared_prefs_manager
issues: https://github.com/aidanlok/shared_prefs_manager/issues

environment:
  sdk: '>=2.17.1 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: